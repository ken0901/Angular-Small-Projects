import { DollarSignPipe } from './dollar-sign.pipe';

describe('DollarSignPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarSignPipe();
    expect(pipe).toBeTruthy();
  });
});
